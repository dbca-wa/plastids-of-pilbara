apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: plastids-ingress
spec:
  ingressClassName: nginx
  rules:
    - host: pilbseq-uat.dbca.wa.gov.au
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: plastids-clusterip-uat
                port:
                  number: 8080
